<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MyBoard.Src.Component.ServerCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyBoard.Src.Component"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border
            Width="200"
            Height="100"
            Background="#FF1E1E1E"
            CornerRadius="12"
            Padding="12"
            BorderBrush="#FF3C3C3C"
            BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{x:Bind ServerInfo.Name, Mode=OneWay}"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="White"/>

                <TextBlock Text="{x:Bind ServerInfo.ConnectionType, Mode=OneWay}"
                           FontSize="12"
                           Foreground="Gray"
                           Margin="0,4,0,0"/>

                <TextBlock Text="{x:Bind ServerInfo.Latency, Mode=OneWay}"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{x:Bind ServerInfo.LatencyBrush, Mode=OneWay}"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Bottom"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
